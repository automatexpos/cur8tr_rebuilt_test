# Supabase Configuration
# Get these from your Supabase project settings

# Project URL - Format: https://xxxxxxxxxxxxx.supabase.co
# Find in: Settings > API > Project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# API Key - Use either anon key (public) or service_role key (admin access)
# Find in: Settings > API > Project API keys
# For development, you can use the anon key
# For production/admin features, use service_role key
SUPABASE_KEY=your_supabase_anon_or_service_role_key

# Database Password - Your Supabase database password
# This is the password you set when creating your project
# If you forgot it, you can reset it in: Settings > Database > Database password
SUPABASE_DB_PASSWORD=your_database_password

# Session Configuration
# Generate a random secret for production (use: openssl rand -base64 32)
SESSION_SECRET=your-random-secret-key-here

# Database URL (direct connection for Supabase)
# Format: postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
# Replace [password] with your SUPABASE_DB_PASSWORD
# Replace [project-ref] with your project reference from SUPABASE_URL
DATABASE_URL=postgresql://postgres:your_password@db.your-project-ref.supabase.co:5432/postgres

# Optional: Port for local development (default: 5000)
# PORT=5000

# Optional: Host for local development (default: 0.0.0.0)
# HOST=0.0.0.0
